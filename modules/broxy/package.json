{
  "name": "broxy",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build src/ --outfile=./dist/broxy.mjs --target=node",
    "format": "biome format src/ --write"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@liode-config/biome": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@effect/cli": "^0.36.35",
    "@effect/platform-bun": "^0.36.0",
    "effect": "^3.2.5"
  }
}
