---
// const withParams = (url: string) => (...params: (string[][] | Record<string, string>)[]) => { 
//     return url + "?" + params.map(ps => new URLSearchParams(ps)).join("&")
// } 
export const prerender = true ;
---

<script type="module">
document.addEventListener("click", async () => {
    console.log("clck")

    // fetch("/apis/tools/gtd", {
    //     method: "POST",
    //     body: JSON.stringify({a: "b"})
    // }).then(res => {
    //     console.log(res)
    //     res.json().then(console.log)
    // })

    // const withParams = (url) => (...params) => { 
    //     return url + "?" + params.map(ps => new URLSearchParams(ps)).join("&")
    // } 

    // const sources = await fetch(withParams("/apis/tools/gtd")({source: "asdf"}))

    // console.log(sources)
})
</script>
